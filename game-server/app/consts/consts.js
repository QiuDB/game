let Consts = function() {
    this.MESSAGE = {
        RES: 200,           // response message code
        ERR: 500,           // error code
        PUSH: 600           // push message code
    }
};

module.exports = {
    id: 'consts',
    func: Consts
}